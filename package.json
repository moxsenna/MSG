{
  "name": "localbiz-ai",
  "version": "1.0.0",
  "description": "Localbiz AI Monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "node packages/scraper/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "lerna": "^8.0.0"
  }
}
